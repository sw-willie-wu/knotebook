# Self-hosting Knotebook

## Deployment prerequisites

- **`PUBLIC_URL` is required.** The server refuses to start without it (or if it isn't a valid `http(s)://` URL). It is used to decide whether session cookies are marked `Secure` and to build the OIDC `redirect_uri` (`<PUBLIC_URL>/api/auth/oidc/callback`) — see [OIDC / SSO login setup](#oidc--sso-login-setup) below.
- **Two supported topologies.** Pick one:
  - **(a) Trusted-LAN plain http.** For a home/office network with no untrusted devices, you can run Knotebook without TLS, reachable by hostname/IP from other machines on the LAN:
    - In `docker-compose.yml`, comment out the default `"127.0.0.1:3000:3000"` port line and uncomment `"3000:3000"` instead (this binds `0.0.0.0`, i.e. every interface).
    - Set `PUBLIC_URL=http://<lan-ip-or-hostname>:3000` (e.g. `http://192.168.1.50:3000`) in `.env`.
    - Understand the trade-off before enabling this: login credentials and the session cookie travel in **cleartext**; the session cookie has **no `Secure` flag**; **anyone on the same network segment can sniff or MITM the traffic**. The server will start and stay up, but it logs a prominent warning line at startup as a reminder — the server logs JSON (pino), so this arrives as one JSON log line with a `SECURITY WARNING: ...` `msg`, not a formatted banner. Only do this on a network where you trust every host, and accept that trade-off knowingly — it is not a substitute for topology (b) on any network you don't fully control.
  - **(b) Public — reverse proxy + TLS.** For anything reachable beyond a trusted LAN (the internet, a shared/untrusted network, etc.), terminate TLS in front of Knotebook and set `PUBLIC_URL` to the public `https://` address — see the next two bullets.
- **Reverse proxy and TLS are bring-your-own (topology (b)).** Knotebook does not terminate TLS itself; put a reverse proxy (nginx, Caddy, Traefik, etc.) in front of it and point `PUBLIC_URL` at the public HTTPS address.
- **Your reverse proxy must forward WebSocket upgrade requests on `/collab`** — real-time collaboration connects same-origin to `wss://<your-domain>/collab`, and a proxy that only forwards plain HTTP will silently break live editing while every other page keeps working. On nginx, the `/collab` location needs `proxy_http_version 1.1;` plus `proxy_set_header Upgrade $http_upgrade;` and `proxy_set_header Connection "upgrade";`; Caddy and Traefik forward WebSocket upgrades automatically with a plain reverse-proxy directive, no extra config needed.
- **`TRUST_PROXY` decides whether `X-Forwarded-For` is believed, and it defaults to off.** Rate limiting and login lockout are keyed on the client address, so the server has to know whether it can trust the forwarding headers in front of it. With `TRUST_PROXY` unset, `X-Forwarded-For` is ignored and the socket's own address is used — the right default when clients can reach the app directly (the trusted-LAN topology, or a public deployment someone can route around). **If you run behind a reverse proxy, set it**, otherwise every visitor arrives as the proxy's address and shares one bucket: five bad passwords from anyone locks out everybody. The server logs a warning the first time it sees an `X-Forwarded-For` header while the setting is off.

  Accepted values: a comma-separated list of trusted proxy addresses — IPs, CIDR ranges, or the names `loopback`, `linklocal`, `uniquelocal` (e.g. `TRUST_PROXY=172.16.0.0/12`); a hop count (`1` for a single proxy); or `true`. A value that doesn't parse fails startup rather than every request. **The list form is the only one that is safe when clients can also reach the app directly** — a hop count and `true` both believe the header whoever sends it, so anyone who can bypass your proxy can forge their address either way.

  **Your proxy has to cooperate, in two ways.** It must send `X-Forwarded-For` — nginx does not do this by default (`proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;`), and it is the only header Knotebook reads for the client address: `X-Real-IP` and RFC 7239 `Forwarded` are ignored, so a proxy that sends only those leaves you with the shared-bucket problem above even with `TRUST_PROXY` set. It should also pass the original `Host` through (`proxy_set_header Host $host;` — Caddy and Traefik already do): image uploads check the browser's `Origin` against the request host, so a proxy that rewrites `Host` to the upstream name and puts the real one in `X-Forwarded-Host` will make uploads fail with 403 unless `TRUST_PROXY` is set.

## Single-instance warning

Only run a single `app` container. `docker compose up --scale app=N` is **not supported** and must not be used.

In-memory state (login rate limiting, the collab-token/slug-change rate limiters, the password-hashing concurrency limiter, and the per-IP rate limiter on the OIDC login/callback endpoints) is already per-process, so scaling out would already produce inconsistent behavior across instances. On top of that, real-time collaboration state (Yjs/Hocuspocus) **now actually lives in server memory**: each note's live document and the set of connected clients are held in-process, with no cross-instance sync. Running multiple `app` instances behind a load balancer would let different clients editing the same note land on different instances, and their edits would **silently diverge** rather than merge or error. There is no clustering/coordination layer, so treat `app` as a single point of deployment, not a horizontally scaled service.

## Compose services and volumes

`docker-compose.yml` (repo root) defines two services:

- **`app`** — built from `docker/Dockerfile`, reads its configuration from `.env` (`env_file: .env`), and by default publishes only on `127.0.0.1:3000` (see topology (a) above to expose it on the LAN instead). It depends on `db` being healthy before starting, and has its own `/healthz`-based healthcheck. Uploaded images are persisted to the `uploads` named volume, mounted at `/app/uploads` — if you customize the compose file and drop that mount, uploaded images vanish on the next container rebuild.
- **`db`** — `pgvector/pgvector:pg17`, with data persisted to the `db_data` named volume. `POSTGRES_PASSWORD` can be overridden via `.env`, but Postgres only applies it while initializing a brand-new (empty) data directory — see the env var reference below.

`docker compose down -v` discards *all* named volumes, not just `db` — it takes the `uploads` volume with it too, so every note and every uploaded image is gone.

## Environment variables

Reference: `.env.example` (repo root) is the canonical source — copy it to `.env` and fill in the values below.

| Variable | Required | Notes |
|---|---|---|
| `DATABASE_URL` | yes | Postgres connection string. With `docker compose`, use the `db` service name as host: `postgres://knotebook:knotebook@db:5432/knotebook`. Outside docker, use `localhost` instead. |
| `APP_SECRET` | yes | 64+ hex characters (`openssl rand -hex 32`). Signs session cookies and collab tokens, and derives the key used to encrypt stored AI provider credentials (see [AI quick actions](./ai.md)) — rotating it invalidates all of those. |
| `PUBLIC_URL` | yes | The externally-reachable base URL. Must be a valid `http(s)://` URL — the server refuses to start otherwise. See [Deployment prerequisites](#deployment-prerequisites) for the two supported topologies. |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` | yes, on first boot (set both) | Creates the first (admin) account at startup — this is the **only** way to initialize a fresh instance; the server refuses to start on an empty/uninitialized database without both set. `ADMIN_PASSWORD` must be 12+ characters. **Only takes effect on first initialization** (empty database, not yet initialized) — once an instance is initialized, these are silently ignored on every subsequent start, with no error or warning (see [Known limitations](./known-limitations.md)). The account created this way must change its password on first login. After first login, consider removing these two lines from `.env` — a plaintext password sitting there is one less secret to leak once the account already exists. |
| `TRUST_PROXY` | no (**set it when you run behind a reverse proxy**) | Whether to believe `X-Forwarded-For`. Unset means the header is ignored and the socket address is used — the safe default when clients can reach the app directly. Behind a proxy, leaving it unset makes every visitor look like the proxy, so they all share one rate-limit and lockout bucket. Accepts a list of trusted proxy addresses (IPs, CIDRs, or `loopback`/`linklocal`/`uniquelocal`), a hop count, or `true`. See [Deployment prerequisites](#deployment-prerequisites). |
| `OIDC_ISSUER_URL` | no (set all three or none) | Base URL of the OIDC identity provider's issuer — Knotebook runs OpenID Connect Discovery against `<issuer>/.well-known/openid-configuration`. Enables OIDC/SSO login when set together with the two variables below. Plain `http://` is allowed only for a trusted-internal IdP, and logs a startup warning when used. See [OIDC / SSO login setup](#oidc--sso-login-setup) below. |
| `OIDC_CLIENT_ID` | no (set all three or none) | Client ID registered with the identity provider for this Knotebook instance. |
| `OIDC_CLIENT_SECRET` | no (set all three or none) | Client secret for the same client registration. Knotebook authenticates with `client_secret_post` (the secret is sent in the token request body) — the client registration must allow that method. |
| `POSTGRES_PASSWORD` (docker-compose.yml, not `.env.example`) | no | Overrides the `db` service's Postgres password (default `knotebook`). Only applied by Postgres while initializing a brand-new (empty) data directory — see [Known limitations](./known-limitations.md) for how to change it after the `db` volume already exists. |

## OIDC / SSO login setup

OIDC login is optional — set `OIDC_ISSUER_URL`, `OIDC_CLIENT_ID`, and `OIDC_CLIENT_SECRET` together in `.env` to enable it (leave all three unset to keep password-only auth; setting only some of them fails the server to start with a clear error). When enabled, a "Continue with SSO" option appears on the login page alongside password login — existing password accounts keep working either way.

When registering the OIDC client for this Knotebook instance on the identity provider side:

- **Grant the `email` and `profile` scopes.** Knotebook requests `openid email profile` on every login — an IdP that doesn't return an `email` claim under this scope can't complete sign-in (`oidc_email_missing`).
- **Allow `client_secret_post` client authentication.** Knotebook sends the client secret in the token request body, not as an `Authorization: Basic` header or a signed client assertion — the client registration must accept that method.
- **Issue an `email_verified` claim.** Knotebook only auto-provisions a new account or links to an existing password account when the IdP reports `email_verified: true`; an IdP that never sends this claim (or always sends `false`) blocks every login (`oidc_email_unverified`).
- **Register the redirect URI as `<PUBLIC_URL>/api/auth/oidc/callback`** (e.g. `https://notes.example.com/api/auth/oidc/callback`) — exactly this path, matching whatever `PUBLIC_URL` is set to.
- **The IdP must sign ID tokens with an asymmetric algorithm** (RS256/ES256/PS256, etc.). This is checked in two stages: if the IdP's discovery metadata doesn't list an asymmetric algorithm at all, OIDC is treated as unavailable before login even starts (redirects with `oidc_unavailable`); if the metadata claims asymmetric support but the ID token actually received back is signed some other way (e.g. HS256), signature verification fails during the code exchange instead (`oidc_exchange_failed`). Either way, an HS256-only IdP isn't supported.

A couple of operational notes:

- Knotebook discovers the IdP's configuration (`<issuer>/.well-known/openid-configuration`, signing keys, supported algorithms) once and caches it for the life of the process. If you change the IdP's endpoints, keys, or algorithm support, **restart the `app` container** to pick up the change.
- Plain `http://` issuer URLs are accepted, for a trusted-internal IdP only, and log a loud startup warning — the same spirit as the plain-http `PUBLIC_URL` topology above: tokens and claims travel in cleartext between Knotebook and the IdP.

See [Known limitations](./known-limitations.md) for the full list of OIDC-specific rough edges (no RP-initiated logout, switching identity providers, the per-IP rate limit on the OIDC endpoints, concurrent multi-tab sign-in, etc.).

## Content Security Policy

Knotebook serves its HTML with a Content-Security-Policy header (plus `Referrer-Policy: no-referrer` and `X-Content-Type-Options: nosniff`). It is not configurable — there is no environment variable for it. Three consequences for a self-hosted deployment:

- **Scripts, stylesheets and fonts must come from Knotebook itself.** A reverse proxy that rewrites the page to inject an analytics snippet or a support widget (nginx `sub_filter`, Cloudflare "Rocket Loader"/apps, and similar) will have that script refused by the browser. The page still works; the injected part silently does not.
- **Assets cannot be moved to a separate CDN.** `/assets/*` and uploaded files are expected to be served from the same origin as the app.
- **Knotebook cannot be embedded in an iframe** (`frame-ancestors 'none'`), so it cannot be placed inside an intranet portal page.

Images, audio and video that a note embeds by URL are deliberately *not* restricted, over both `http:` and `https:` — see [External images embedded by URL](known-limitations.md) for what that means for a reader's IP address.

If one of the restrictions above blocks something you need, the escape hatch is your own reverse proxy: override or remove the `Content-Security-Policy` response header there (nginx: `proxy_hide_header Content-Security-Policy;` plus your own `add_header`). Removing it gives up the protection for every user of that instance.

## Troubleshooting

- **Server crash-loops on first boot with a database error mentioning `users_email_unique`.** This means `instance_setup` is still empty (the instance believes it hasn't been initialized yet) but a `users` row already exists whose stored `email` is exactly the lowercase form of `ADMIN_EMAIL` — `users.email` has a plain unique constraint on the raw column, not a `lower()` index, so this only collides when the existing row happens to already be lowercase (a mixed-case existing row wouldn't collide here, but would instead become one of the pre-v0.1 duplicate-row cases in [Known limitations](./known-limitations.md) if a fresh lowercase row got created too). This typically happens after restoring a partial backup, or after the `instance_setup` row was removed manually. `initializeInstance` retries the same failing insert on every restart, so the container keeps crashing identically. Fix it one of two ways: change `ADMIN_EMAIL` in `.env` to an address not already present in `users`, or, if that existing row is meant to be the admin account, mark the instance initialized directly instead of relying on the env bootstrap (`INSERT INTO instance_setup (singleton) VALUES (true) ON CONFLICT DO NOTHING;` and `UPDATE users SET is_admin = true WHERE email = '<lowercased-admin-email>';`), then restart — `ADMIN_EMAIL`/`ADMIN_PASSWORD` are ignored once `instance_setup` has a row.

- **Everyone gets `429 too_many_attempts` on login, including people typing the right password, and it does not clear on its own.** The per-IP half of the login lockout is keyed on `request.ip`. Behind a reverse proxy with `TRUST_PROXY` unset (or with a proxy that does not send `X-Forwarded-For`), that is the proxy's address for **every** visitor — so one person's failed attempts count against everybody, and the backoff window keeps being extended by the next failure from anyone. Fix it by setting `TRUST_PROXY` (and making sure the proxy sends `X-Forwarded-For`, see [Deployment prerequisites](#deployment-prerequisites)), then **restart the `app` container**: the counters live in process memory, so without a restart you still have to wait out a full 15-minute window with no failed login from anyone. The server logs a `SECURITY WARNING` at startup when `PUBLIC_URL` is https and `TRUST_PROXY` is unset, and warns once at runtime when it sees a forwarding header it was told not to trust.

- **Someone reports being kicked out of a note ("You no longer have access"), or a tab that shows "Connecting…" and then a "Not loaded — offline" / "Editing offline" warning and never recovers.** Refused collaboration connections are logged by the `app` container with structured fields `phase`, `noteId`, `userId`, `cause` and `reason` — `docker compose logs app | grep '"cause"'` shows them, and every line at the default level names the account it refused. `phase` says when it happened: `handshake` (refused while connecting), `reverify` (the connection was open and was closed after a permission re-check — this is what a revoked share looks like), or `deadline` (the connection was closed because the client did not answer a re-check within five seconds, which is how a legitimate user gets ejected when their own token fetch is stuck). The `cause`:
  - `no-role` — the account genuinely has no role on that note. **This is the one that means what the "You no longer have access" notice says.**
  - `note-deleting` — the note was deleted, or is being deleted right now. The tab shows "note deleted" and returns to the list. Only people who could see the note when the delete started, or who still hold a role on it, are told this; to anyone else it is refused with the same `forbidden` reason as a note that never existed. The window is bounded (see `DELETING_GATE_TTL_MS`, currently two minutes — and it lives in process memory, so restarting the `app` container ends it early); a tab reconnecting after that gets `no-role` instead, because once the record is gone the server has no way to tell "deleted" from "not yours".
  - `session-revoked` — the account was disabled or deleted, or its sessions were invalidated (a password change). This is **not** an access decision about the note: the tab fetches a new token within seconds, that request 401s, and the tab signs itself out. Check `users.disabled_at` and `users.token_version`.
  - `no-reverify` (`phase: deadline`) — the client did not re-present its token in time. The person is ejected exactly as if access were revoked, so if they insist they still have access, this is the line that says otherwise.
  - `bad-token` — the token was missing, failed to verify, or was bound to a different note. **In `phase: reverify` this ejects the person and is logged at the default level. In `phase: handshake` it is logged at `debug`, which this server does not emit at all**: it is the only refusal reachable before the token identifies anyone, and `/collab` accepts WebSocket upgrades without a session, so logging it would let an anonymous client decide how much disk your logs consume. A handshake stuck on `bad-token` therefore looks, from the browser, like a tab that shows "Connecting…" and then (after a few seconds) "Not loaded — offline", with nothing in the log.
  - `server-error` — the handshake hit an unexpected error, logged at error level with the stack trace and the same `cause` field. This is the usual reason a tab retries indefinitely (badge stuck on "Connecting…" → "Not loaded — offline") with something to find; the usual culprit is the database being unreachable.

  Refusals decided by the handshake and by a re-check are capped per account (ten a minute, with errors on their own budget); when the cap bites, one `collab 拒連日誌已達每分鐘上限` warning is emitted per account per minute so an empty grep is never ambiguous. The cap exists because a single valid token can be replayed on one WebSocket indefinitely. `phase: deadline` lines are not capped; they are bounded only by how many connections a re-check covers, and one is written per connection actually closed that way. One further log-volume path is intercepted: message types the collaboration library reacts to with an unstructured error line and an open connection — unknown types, the enum-listed-but-unhandled `SyncStatus`/`Ping`/`Pong`, and client-sent authentication messages of kinds only servers send — are dropped before they reach the library, and a connection that accumulates ten of them is forcibly disconnected (`collab 連線持續送出無法處理的訊息型別，強制斷線`, capped process-wide at ten lines per minute). The compose file also caps the `app` container's log size (50 MB × 5 rotated files) as a backstop. None of this makes the collab endpoint immune to a signed-in account generating log volume by other means.

- **AI quick actions stopped working right after someone edited a provider's Base URL.** Changing the Base URL clears that provider's stored API key on purpose — the key is sent to whatever host the field names, so it is not carried over to a new one (see [AI setup](./ai.md)). Fix it by entering the API key again in Settings; the provider card says "No API key stored" until you do. If nobody meant to change the URL, `docker compose logs app | grep 'base URL'` shows every edit that carried one — the Settings form always sends the field, so a line appears even when the value did not change; compare `from` and `to` — along with the `userId` that made it (a uuid — look it up in the `users` table to put a name to it).

- **The log shows `筆記內容含非 http(s) scheme 的媒體 URL`.** A collaborator with edit access wrote a media URL with a scheme like `javascript:` or `data:` directly into a note's collaborative document (the input field refuses these, but the document itself is writable by any editor). The URL is harmless inside Knotebook — rendering and export replace it before it reaches anything (the one exception is the app-private `blocknote/html` clipboard format, which only another BlockNote can read and which re-enters the render guard on paste) — but the string itself stays in the document until someone deletes that block, and this line is how you find out it exists. The fields name the note (`noteId`), how many were found (`findings`), the schemes and the block types; the URL itself is deliberately not logged. A note that stays open logs one line however often it saves; closing and reopening it (or restarting the server) produces one more — so a collaborator reconnecting in a loop can still generate lines at the rate of their reconnects. Unlike the refusal lines above, this line has no per-account budget; each line's size is bounded (block names truncated, at most ten distinct schemes/blocks listed), but the line count under reconnect churn is not. The line keeps reappearing until the offending block is removed. To locate the block, open the note and look for a broken image/player or a file link — those are the guarded renderings of such URLs.

## See also

- [Known limitations](./known-limitations.md) — the full list, including several deployment/env-var gotchas referenced above.
- [Restoring note content from a backup](./backup-restore.md).
- [API contract summary](./api.md).
