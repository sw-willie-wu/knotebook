# Database connection URL
# Format: postgres://user:password@host:port/dbname
# For local development with docker-compose, use the service name: postgres://knotebook:knotebook@db:5432/knotebook
# For local development without docker, use: postgres://knotebook:knotebook@localhost:5432/knotebook
DATABASE_URL=postgres://knotebook:knotebook@db:5432/knotebook

# Application secret for signing and encryption
# Must be 64+ hexadecimal characters (e.g., output of `openssl rand -hex 32`)
# Generate with: openssl rand -hex 32
APP_SECRET=

# Public URL for the application
# Use https for public deployments (behind a reverse proxy that terminates TLS)
# Plain http is also allowed on a trusted LAN (e.g. http://<lan-ip>:3000) — the
# server will start but logs a loud warning; see docs/self-hosting.md "Deployment prerequisites"
# Example for local: http://localhost:3000
# Example for trusted LAN: http://192.168.1.50:3000
# Example for public: https://notes.example.com
PUBLIC_URL=

# Optional: Email address accepted as the first admin account
# If set, the initial setup page will only accept this email address for creating the first admin account (rejects with 403 otherwise)
# If not set, any email can be used to create the first admin account
# BOOTSTRAP_ADMIN_EMAIL=

# Optional: skip the setup page entirely by creating the first admin account from these two
# variables at startup. Set BOTH or NEITHER — setting only one fails the server to start
# with a clear error. ADMIN_PASSWORD must be 12+ characters.
#
# This only takes effect on first initialization (empty database, setup not yet completed) —
# same behavior as POSTGRES_PASSWORD in docker-compose.yml (see docs/known-limitations.md):
# once an instance is set up, these variables are silently ignored on every subsequent start,
# no error or warning. The account created this way must change its password on first login
# (see the mustChangePassword note in docs/known-limitations.md).
# After first login, consider removing these two lines from .env — leaving a plaintext
# password there is no longer useful once the account exists, and it's one less secret to leak.
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
